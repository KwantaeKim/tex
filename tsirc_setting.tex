% --------------------------------------------- %
% Packages and Configurations
% --------------------------------------------- %
\usepackage[pdftex]{graphicx}   % images
\usepackage[table]{xcolor}      % table and colors
\usepackage{multirow}           % merged multi-row table cells
\definecolor{darkgreen}{rgb}{0.0, 0.5, 0.0}     % you can use by: \textcolor{darkgreen}{text}
\definecolor{darkred}{HTML}{C80000}
\definecolor{darkyellow}{HTML}{F5B301}
\definecolor{darkblue}{RGB}{0, 79, 144}         % primary blue (figures, tables)
\definecolor{deepblue}{HTML}{1F4E79}            % darkest blue tier (e.g., storage)
\definecolor{navy}{HTML}{16243A}                % near-black navy (text)
\definecolor{darkpurple}{RGB}{138, 99, 201}     % purple accent
% justified text: flush on both left and right margins (Word's "Justify")
\hyphenpenalty=10000            % no word hyphenation at line breaks
\exhyphenpenalty=10000
\emergencystretch=3em           % absorb long unbreakable tokens instead of overflowing

\usepackage{enumitem}           % bullet items
\setlist{nosep}                 % no space between list items

\usepackage{hyperref}   % links
\hypersetup{colorlinks=true, urlcolor=cyan, citecolor=cyan, linkcolor={}}

\usepackage[sfdefault]{carlito} % font

\usepackage{setspace}           % line spacing
\singlespacing                  % 1.5 spacing

% page margins
\usepackage[left=20mm, top=20mm, bottom=20mm, right=20mm]{geometry}

% Setting spacing for section, subsection and paragraph
\usepackage{titlesec}
\titleformat*{\section}{\large\bfseries}
\titleformat*{\subsection}{\bfseries}
\titlespacing{\section}{0 cm}{6pt}{0pt}
\titlespacing{\subsection}{0 cm}{6pt}{0pt}
\titlespacing{\subsubsection}{0 cm}{6pt}{0pt}
\setlength{\parskip}{6pt}
\setlength{\parindent}{0pt}     % no paragraph indentation

% header and footer
\usepackage{fancyhdr}
\pagestyle{fancyplain}
\fancyfoot{}
\fancyhead{}
\setlength{\headheight}{30pt}       % to avoid warning
\addtolength{\topmargin}{-2.5pt}    % to avoid warning
\renewcommand{\headrulewidth}{0pt}  % remove line
\lhead{\raisebox{-0.8em}{\href{https://kwantaekim.github.io}{\includegraphics[height=1cm]{\LogoPath}}}}
\chead{\docname}
\rhead{page \fancyplain{}{\thepage} ({\pageref{lastpage}})}
\lfoot{TSirc group}
\rfoot{Aalto University}

\usepackage{fontawesome5}       % for using icons
\usepackage{amsmath}            % for math
\usepackage{amssymb}            % for math

% Inline marks for good / bad examples
\newcommand{\exbad}{\textcolor{darkred}{\faTimes}}
\newcommand{\exgood}{\textcolor{darkgreen}{\faCheckCircle}}
\newcommand{\exeg}{\textcolor{darkyellow}{\faLightbulb}}
\newcommand{\exq}{\faQuestionCircle}
\newcommand{\exa}{\faCommentDots}
% Circled number, e.g. \circnum{1}
\newcommand{\circnum}[1]{\tikz[baseline={([yshift=-0.7ex]c.center)}]{\node[circle, fill=darkred, text=white, inner sep=0.5pt, minimum size=1.2em, font=\scriptsize\bfseries] (c) {#1};}}

% --------------------------------------------- %
% code representation

\usepackage{tcolorbox}
\usepackage{minted}
\setminted{
    frame=lines,
    framesep=2mm,
    baselinestretch=1.2,
    fontsize=\footnotesize,
    linenos
}

\renewcommand{\texttt}[1]{{\footnotesize\ttfamily #1}}      % smaller font for texttt
\newcommand{\sverb}[1]{\colorbox{gray!10}{\texttt{\detokenize{#1}}}}      % smaller font for verbatim
% for proper font of code representation
\usepackage{lmodern}
\usepackage[T1]{fontenc}

% --------------------------------------------- %
% table of contents

\usepackage{tocloft}

% Remove "Contents" title
\renewcommand{\contentsname}{}

% Make Section Titles Bold and Bigger in ToC
\renewcommand{\cftsecfont}{\bfseries\large}
\renewcommand{\cftsubsecfont}{\bfseries}

% Add Dotted Lines Between Titles and Page Numbers
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}

% --------------------------------------------- %
% vertical alignment
\usepackage{adjustbox}

% --------------------------------------------- %
% graph drawing
\usepackage{tikz}
\usetikzlibrary{arrows.meta}    % filled triangular arrow tips
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% --------------------------------------------- %

\usepackage{kotex}
